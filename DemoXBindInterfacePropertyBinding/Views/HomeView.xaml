<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="DemoXBindInterfacePropertyBinding.HomeView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DemoXBindInterfacePropertyBinding"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    x:Name="CurrentPage">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Button Command="{x:Bind ViewModel.RunCalculateMethodCommand}">Start Task</Button>
        <TextBlock Grid.Row="1" Text="{x:Bind ViewModel.InterfaceWithObservableProperty.Status, Mode=OneWay}" />
        <TextBlock Grid.Row="2" Text="{Binding ElementName=CurrentPage, Path=ViewModel.InterfaceWithObservableProperty.Status, Mode=OneWay}" />
    </Grid>
</Page>
